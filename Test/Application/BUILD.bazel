load("//:defs.bzl", "fw_test_suite")

fw_test_suite(
    deps = ["@futurewalker//:Application", "//Source/Test"],
    tags = ["Application"],
    names = [
        "BoxConstraints",
        "CornerRadius",
        "EdgeInsets",
        "View",
    ],
)
