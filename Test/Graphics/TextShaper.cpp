#include <catch2/catch_all.hpp>

#include <Futurewalker.Graphics.TextShaper.hpp>
#include <Futurewalker.Graphics.ShapedText.hpp>

using namespace Futurewalker;

TEST_CASE("TextShaper")
{
    /*
    auto shaper = Graphics::TextShaper::Make();
    REQUIRE(shaper);

    SECTION("Empty")
    {
        auto const shaped = shaper->Shape(u8"", {}, 16, 0.0f);
        REQUIRE(shaped);
        REQUIRE(shaped->GetBounds().IsEmpty());
    }

    SECTION("Hello")
    {
        auto const shaped = shaper->Shape(u8"Hello, TextShaper!", {}, 16, 0.0f);
        REQUIRE(shaped);
        REQUIRE(!shaped->GetBounds().IsEmpty());
    }
    */
}
