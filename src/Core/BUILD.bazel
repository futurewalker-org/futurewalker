load("//:defs.bzl", "fw_library")

fw_library(
    name = "Core",
    visibility = ["//visibility:public"],
    deps = [
        "@boost//:boost-headers",
        "@fmt//:fmt",
        "@icu//:icu",
    ],
)
