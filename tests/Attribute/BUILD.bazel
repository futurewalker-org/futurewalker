load("//:defs.bzl", "fw_test_suite")

fw_test_suite(
    deps = ["@futurewalker//:Attribute"],
    tags = ["Attribute"],
    names = [
        "AttributeAccessor",
        "AttributeArg",
        "AttributeNode",
        "AttributeValue",
    ],
)
