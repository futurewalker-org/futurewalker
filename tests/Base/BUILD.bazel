load("//:defs.bzl", "fw_test_suite")

fw_test_suite(
    deps = ["@futurewalker//:Base"],
    tags = ["Base"],
    names = [
        "Base",
        "Locator",
        "UniqueIdentifierManager",
    ],
)
